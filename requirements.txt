# Python 3.9 or newer.
# Only two third-party packages are used; everything else is standard library
# (abc, dataclasses, time). The numpy floor is set by np.random.default_rng.
numpy>=1.17
matplotlib>=3.5
